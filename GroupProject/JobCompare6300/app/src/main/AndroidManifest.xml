<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.jobcompare6300">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobCompare6300">
        <activity android:name=".Application">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CurrentJobEditor">
        </activity>

        <activity android:name=".JobOfferComparer">
        </activity>

        <activity android:name=".SettingsEditor">
        </activity>

        <activity android:name=".JobOfferEditor">
        </activity>

        <activity android:name=".JobRanker">
        </activity>
    </application>

</manifest>